import React from 'react';

/**
 * CountdownGate - Simplified
 * Removed the locking functionality as per user request to only have countdowns on the landing page.
 */
const CountdownGate = ({ children }) => {
    return <>{children}</>;
};

export default CountdownGate;
